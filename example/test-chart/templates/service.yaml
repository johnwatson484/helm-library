{{- include "helm-library.service" (list . "test-chart.service") -}}
{{- define "test-chart.service" -}}
# configuration to be merged with manifest generated by the library
metadata:
  annotations:
    prometheus.io/scrape: "true"
{{- end -}}