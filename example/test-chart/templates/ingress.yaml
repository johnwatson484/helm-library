{{- if .Values.ingress.enabled }}
{{- include "helm-library.ingress" (list . "test-chart.ingress") -}}
{{- end }}
{{- define "test-chart.ingress" -}}
# configuration to be merged with manifest generated by the library
{{- end -}}
