{{- if .Values.ingress.enabled }}
{{- include "helm-library.ingress" (list . "chart.ingress") -}}
{{- end }}
{{- define "chart.ingress" -}}
# configuration to be merged with manifest generated by the library
{{- end -}}
